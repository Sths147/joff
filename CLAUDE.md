## Agent skills

### Session start

At the start of every session, invoke the `token-efficient` skill.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
